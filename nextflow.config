params {
    samples = 'config/samples.tsv'
    results = 'results'
    threads = 10
    target_gene = 'blaCTX-M-15'
}

process {
    cpus = params.threads
    shell = ['/bin/bash', '-euo', 'pipefail']
}

profiles {
    dryrun {
        process.executor = 'local'
    }
}
